# googleConvertToEmbedImage

Use to take a text file of google drive links for images and convert them to ready embed links for easy display

Must use a text file named "test.txt" in the same directory
